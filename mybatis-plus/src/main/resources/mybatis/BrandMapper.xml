<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.mybatisplus.mapper.BrandMapper">

        <select id="findById" resultType="com.mybatisplus.com.security.session.pojo.Brand">
          select * from tb_brand where id = #{id}
        </select>

    </mapper>
